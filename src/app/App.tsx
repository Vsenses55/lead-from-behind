import PrototypePage from "./pages/PrototypePage";

// This app is currently just the single Prototype page — the design-system
// tabs (Components/Colors/Typography/Spacing) and their router are on hold
// for now. See routes.ts if that navigation comes back later.
export default function App() {
  return <PrototypePage />;
}
